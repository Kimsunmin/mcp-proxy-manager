# 원본 MCP 서버의 SSE 엔드포인트
# (실제 실행 중인 MCP 서버 주소로 변경해주세요. 보통 /sse로 끝납니다)
ORIGINAL_MCP_URL=

# (선택) 원본 MCP 서버의 메시지 엔드포인트
# 설정하지 않으면 ORIGINAL_MCP_URL에서 /sse를 /mcp/messages로 바꿔서 자동으로 사용합니다.
ORIGINAL_MCP_MESSAGE_URL=

# 클라이언트(Claude 등)가 접속할 이 프록시 서버의 주소 (참고용)
NEXT_PUBLIC_PROXY_URL=http://localhost:3000/api/mcp/v1/sse
